addressbook/
│
├── include/
│   └── addressBook.h         // Declarations for all functions
│
├── functions/
│   ├── addEntry.c            // addEntry()
│   ├── deleteEntry.c         // deleteEntry()
│   ├── displayEntries.c       // displayAllEntries()
│   ├── editEntry.c           // editEntry()
│   ├── fileOperations.c       // saveAndExit(), isDuplicate(), printContactsBySearch(), compareContacts()
│   ├── sortData.c            // sortdata()
│   ├── searchEntry.c         // searchEntry()
│   ├── validate.c            // getName(), getAddress(), getPhoneNumber(), getEmail()
│   └── printUtils.c          // reset_color(), set_color(), print_with_animation2(), printCyberAttackTitle()
│
└── main.c                    // displayMenu(), and call all the main functions




i can make one change that i i can give csv path in main and can access with all the functions